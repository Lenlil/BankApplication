@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

    @*<ul class="nav-menu">*@
        @if (SignInManager.IsSignedIn(User))
        {
            @*<li >
                <a  asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">User Account</a>
            </li>*@            
            <li>
                <form id="LogoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "Identity" })">
                    <a href="#" onclick="document.getElementById('LogoutForm').submit()">Log out</a>
                </form>
                @*<form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button class="nav-menu" type="submit">Logout</button>
        </form>*@
            </li>
        }
        else
        {
            @*<li class="nav-item">
                <a  asp-area="Identity" asp-page="/Account/Register">Register</a>
            </li>*@
            <li>
                <a  asp-area="Identity" asp-page="/Account/Login">Login</a>
            </li>
        }
    @*</ul>*@
